services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dke
      POSTGRES_PASSWORD: dke
      POSTGRES_DB: dke
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
